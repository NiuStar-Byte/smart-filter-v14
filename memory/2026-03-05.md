# 2026-03-05 - Daemon Persistence Bug Discovered

## Critical Issue: Signals Fire But Don't Persist to SENT_SIGNALS.jsonl

### Discovery (Session End)
- **Status**: ACTIVE INVESTIGATION
- **Severity**: CRITICAL - Breaks real-time signal tracking and reporter freshness
- **Evidence**:
  - Daemon process running (PID 52000)
  - Signal activity visible in logs (VOXEL-USDT, ARK-USDT alerts to Telegram confirmed)
  - **BUT**: SENT_SIGNALS.jsonl timestamp frozen at 2026-03-05T02:39:57 (UTC)
  - **Latest signal in file**: 95 NEW signals, last at 09:39:57 GMT+7 (02:39:57 UTC)
  - **Current time**: Much later, but no new file updates

### Root Cause Hypothesis
- `log_sent_signal()` function in daemon NOT persisting to disk in real-time
- Signals generated in-memory but append-to-file not working
- Possible causes: File permissions, file path error, buffering issue, or function not called

### Files Under Investigation
- `/Users/geniustarigan/.openclaw/workspace/smart-filter-v14-main/signal_sent_tracker.py` (should auto-tag with signal_origin: "NEW")
- `/Users/geniustarigan/.openclaw/workspace/smart-filter-v14-main/main.py` (daemon entry point, calls signal_sent_tracker)
- `/Users/geniustarigan/.openclaw/workspace/SENT_SIGNALS.jsonl` (target file for writes, currently stale)

### Next Steps (Priority Order)
1. Inspect `signal_sent_tracker.py` - verify `log_sent_signal()` implementation
2. Check `main.py` - confirm `log_sent_signal()` being called for each new signal
3. Verify file permissions on SENT_SIGNALS.jsonl (must be writable)
4. Verify file path is absolute and correct
5. Test manual signal write to isolate if issue is code-level or OS-level
6. Once fixed, verify new signals appear in reporter with current timestamps

### Architecture Context
- **Unified file**: Single SENT_SIGNALS.jsonl with `signal_origin` markers (FOUNDATION or NEW)
- **Current state**: 1,087 total signals (992 FOUNDATION + 95 NEW)
- **Expected behavior**: Daemon appends new signals in real-time with auto-tagged `signal_origin: "NEW"`
- **Actual behavior**: No new appends since 02:39:57 UTC

### Impact
- Reporter shows stale data (frozen at Mar 5 02:39:57 UTC)
- New signals not included in live tracking
- A/B test performance tracking incomplete
- Phase 2-FIXED decision threshold potentially incorrect if new signals lost

### GitHub Status
- ✅ All commits pushed and synced (zero divergence)
- ✅ Architecture #3 (unified file) committed and live
- ✅ Daemon auto-tag logic committed
- ⚠️ Logic implemented but NOT EXECUTING correctly (persistence broken)

---

## Session Summary
### Completed This Session
- Fixed daemon hang (in-memory caching, 254s → stable)
- Established unified data architecture (single SENT_SIGNALS.jsonl with signal_origin field)
- Locked PEC Enhanced Reporter template (all 8 sections, multi-layered protection)
- Implemented Local/GitHub Sync Protocol (mandatory push after each commit)
- Created 8 monitoring commands for tracking all systems
- Verified all SUMMARY metrics are 100% DYNAMIC (no hardcoding)

### Blocker Discovered (End of Session)
- **Daemon persistence not working** - signals generated but not written to file
- Prevents reporter from showing real-time signal data
- Must be fixed before Phase 2-FIXED decision can proceed
