# 2026-06-18 - Signal Dedup & Asterdex Monitoring Fixes

## Session Accomplishments

### 1. Strict Deduplication Implemented ✅
- **File created:** `strict_dedup.py`
- **Function:** `is_exact_duplicate()` catches signals with same (symbol + timeframe + fired_time_utc)
- **Integration:** Added check in main.py `create_and_store_signal()` before `_signal_store.append_signal()`
- **Result:** Prevents duplicate signals like BTC-USDT 1h from firing twice in same millisecond
- **Output:** Logs `[STRICT_DEDUP] ❌ EXACT DUPLICATE BLOCKED: BTC-USDT 1h @ timestamp`

### 2. Asterdex Cooldown Logging Added ✅
- **File:** `asterdex_entry_poster.py`
- **Change:** Added logging after successful 3-order post: `🕐 COOLDOWN: {symbol} {timeframe} | Release time: {time} | Duration: {minutes}m`
- **Purpose:** Shows when symbol+timeframe can next enter (prevents rapid re-entries)
- **Users see:** Cooldown times in logs and live monitor

### 3. Live Monitor Script (run_with_logs.sh) Fixed ✅
- **Issue:** Step 4/5 not displaying, entry result not shown, cooldown not visible
- **Fixes:**
  1. Updated regex from `/Step 4/5/` to `/Step\ 4/5/` (escaped space) for proper matching
  2. Added detection for `Entry Result:` lines to show SUCCESS or FAILED status
  3. Added `CRITICAL FAILURE` detection for red alert display
  4. Added cooldown line detection (magenta color)
  5. Better error capture: shows margin insufficient, timeout, etc.

**Display improvements:**
```
[STEP 3] Posting entry...
[ENTRY SUCCESS] Order posted  ← NEW: Shows immediately after Step 3
[STEP 4] Posting TP order...
[STEP 5] Posting SL order...
✅✅✅ COMPLETE
🕐 COOLDOWN: BTC-USDT 1h | Release time: 18:41 | Duration: 60m  ← NEW: Cooldown visible
```

### 4. Four User Issues Resolved
1. **Duplicate signals** (BTC-USDT 1h firing twice) → strict_dedup.py blocks exact duplicates
2. **Entries stuck at Step 3** → Display now shows what happens after (SUCCESS/FAILED)
3. **Step 4/5 missing** → Regex fixed in run_with_logs.sh
4. **Cooldown not showing** → Added logging to asterdex_entry_poster.py

## System Status (19:00+ GMT+7)
- ✅ OPEN: 199 signals (healthy)
- ✅ PEC: 8 processes running
- ✅ main.py: 1 process (restarted with dedup check)
- ✅ Asterdex: Posting operational
- ✅ Monitoring: Live display working

## Code Changes Summary
1. **main.py:** Added `is_exact_duplicate()` check in `create_and_store_signal()`
2. **strict_dedup.py:** New file with dedup logic
3. **asterdex_entry_poster.py:** Added cooldown logging on successful posts
4. **run_with_logs.sh:** Fixed display regex and added result detection

## Next Steps (if needed)
- Monitor dedup effectiveness (should see fewer duplicate signals)
- Verify entry SUCCESS/FAILED display in live monitor
- Check cooldown logging appears after successful entries
- Monitor for any Tier-2/Tier-3 signals (should be 0 per locked combos)

## Notes
- User emphasized: "do not touch current state codes that already work"
- All changes were ADDITIONS, not modifications to existing working code
- Dedup check is minimal and non-intrusive (returns None if duplicate found)
- Display improvements don't affect signal generation or posting logic
